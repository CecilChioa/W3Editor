---
description: W3Editor 64 位现代化 War3 地图编辑器项目规则
alwaysApply: true
---

# W3Editor 项目规则

## 项目目标

本项目是 64 位现代化 Warcraft III / YDWE 编辑器，第一阶段只做地形编辑闭环：新建地图、打开地图、渲染地形、编辑地形、保存地图、重新打开验证。

## 技术路线

- 主技术路线固定为 C++20、Qt 6 Widgets、CMake、vcpkg。
- C++20 是工程基线，所有核心模块必须在 C++20 下稳定编译。
- C++23 特性只允许在独立工具或局部模块中谨慎使用，且不得成为核心工程硬依赖。
- 不使用 C++26 特性作为项目代码依赖。
- 构建目标默认为 Windows x64。
- 核心格式、地图、地形、渲染模块必须保持 UI 无关，避免底层能力绑定到 Qt 控件。
- 旧项目 `C:\war3\mytool\W3EditorOld` 只作为参考，不直接延续旧架构。
- UI 参考 `C:\war3\mytool\W3Editor\界面参考`。

## 工作区权限边界

- 完全授权可写范围仅限 `C:\war3\mytool\W3Editor\` 及其子目录。
- `C:\war3\mytool\W3Editor\` 之外的任何目录仅允许只读访问或复制文件到本项目目录内，不允许修改、删除、覆盖或生成文件。
- 旧项目 `C:\war3\mytool\W3EditorOld`、YDWE/FIM 相关目录和其他本地项目均视为外部只读参考源。
- 如需使用外部参考文件，必须复制到 `C:\war3\mytool\W3Editor\` 下的明确位置后再处理。
- 不得对外部参考目录执行清理、格式化、批量重命名、构建输出或测试输出。

## 项目文档维护

- 项目规则和 `W3EditorNext规划.md` 必须随实现进度及时更新，不允许让文档长期停留在过期假设上。
- 当架构边界、阶段目标、依赖选择、测试样本、构建方式、CLI 行为、日志观测或 UI 规则发生变化时，必须同步更新规则文件和规划文档。
- 若代码实现与既有规划发生冲突，先判断是实现偏离还是规划过期；确认规划过期时，应以当前可验证实现和用户最新决策为准修正文档。
- 每个阶段完成后，应在规划文档中记录已完成能力、未完成风险、下一阶段入口和验收方式。

## Git 提交、推送与忽略规则

- 只提交和推送已完成构建、测试或明确人工验证的代码与文档；未验证实验代码不得进入主分支。
- 推送前必须检查 `git status` 和待提交 diff，确认没有构建产物、日志、诊断快照、临时文件或外部参考目录内容混入。
- `.gitignore` 必须覆盖 `build/`、`build/artifacts/`、`build/logs/`、`diagnostics/`、CMake/vcpkg 生成目录、IDE 本地配置、二进制产物和 round-trip 派生产物。
- `testmap` 中的原始样本地图作为第一阶段测试输入管理，不得被测试覆盖；由测试生成的地图、dump、日志和诊断文件不得提交。
- 提交或推送前如发现规则、规划或 `.gitignore` 与当前工程状态不一致，必须先同步更新。

## 测试样本

- 第一阶段真实地图包样本目录固定为 `C:\war3\mytool\W3Editor\testmap`。
- 当前样本包括 `九种兵器3.w3x`、`testdemo.w3x`、`testmap1.w3x`。
- 自动化测试应优先从该目录读取 `.w3x` 样本，直接通过本项目 Archive/MPQ 能力读取包内文件。
- 测试不得覆盖 `testmap` 中的原始样本地图；round-trip、导出和日志产物必须写入 `build/artifacts/` 或 `build/logs/`。

## 架构边界

- `formats` 只做 War3 文件格式读写，不依赖 UI，不做编辑逻辑。
- `terrain` 只做地形数据模型、刷子、编辑命令、撤销重做，不直接读写地图文件。
- `render` 只做渲染，不解析二进制格式。
- `resources` 负责模型、贴图、SLK/BLP/MDX 资源索引、缓存、批量预览数据准备和资源生命周期管理，不允许塞进 UI 或地形模型。
- `preview` 负责模型批量预览、缩略图/预览缓存、异步加载队列和预览状态，不直接修改地图数据。
- `apps/editor` 只做 Qt 窗口、菜单、工具栏、面板和交互。
- `map` 作为文档层，统一管理打开、保存、dirty 状态和地图生命周期。

## 地形编辑支撑能力

- 地形编辑不能只规划高度和贴图刷子，必须同步规划模型批量预览、资源管理、资源索引和预览缓存。
- 批量预览必须支持装饰物、单位、地形装饰、贴图和水面相关资源的快速筛选、缩略图/预览状态和异步加载。
- 资源管理必须通过独立 `resources` 模块接入 Archive、BLP、SLK、MDX/模型、图标和后续对象表，不允许 UI 层直接散落读取资源文件。
- 预览和资源加载必须优先可测试：资源索引、缓存命中、缺失资源回退、批量加载取消、错误日志都要有自动化测试或 CLI 验证入口。

## 第一阶段优先级

1. Archive/MPQ 自主读取：能直接打开 `.w3x/.w3m`，并从包内读取 `war3map.w3e`，不得依赖外部解包工具。
2. `war3map.w3e` 读写与 round-trip 测试：输入来源必须是 `.w3x/.w3m` 包内文件或本项目测试夹具。
3. 地图目录打开作为调试与派生产物支持，不作为唯一主路径。
4. 地形模型 `TerrainModel`。
5. 地形显示。
6. 抬高、降低、平整、平滑、贴图刷、水面。
7. 保存地图并重新打开验证；目录地图和 `.w3x/.w3m` 另存都必须走本项目 Archive 抽象。

## 代码生成要求

- 不要把 UI、格式解析、渲染、编辑逻辑混写在同一个类里。
- 避免单文件过重；当单个源文件持续膨胀到难以审查、测试或维护时，必须按职责拆分到 Reader/Writer、Model、Controller、Service、Cache、ViewModel、Widget 等独立文件。
- 新增二进制格式 Reader/Writer 时，必须提供 round-trip 测试入口或测试计划。
- 写保存逻辑时必须考虑自动备份，默认不破坏原图。
- 任何地图文件写入都必须通过 `WarMapDocument` 或 Archive 抽象，不允许 UI 层直接写文件。
- 错误处理使用明确结果类型或异常边界，不允许静默失败。
- 日志要记录关键地图文件缺失、解析失败、保存失败、备份路径。
- 拥有完整的中文注释，便于后续阅读和修改。

## 测试与重规划规则

- 优先自动测试；能用 CTest、CLI、round-trip、JSON dump 或资源索引校验覆盖的问题，不应只依赖人工 UI 验证。
- 同一问题经过多次自动测试和修复仍无法稳定解决时，必须停止盲目试错，回读 `W3EditorOld`、`YDWE-master` 或相关参考项目，重新确认格式语义、资源链路和架构边界后再调整方案。
- 重规划必须记录触发原因、已失败的验证方式、参考项目证据、调整后的模块边界和新的验收方式。
- 反复失败的问题应优先收敛成最小可复现 CLI/单元测试，再进入 UI 联调。

## 命名约定

- 类型名使用 PascalCase，例如 `TerrainModel`、`W3eReader`。
- 函数和变量使用 camelCase，例如 `openMap`、`tileCount`。
- 文件名与主类型一致，例如 `TerrainModel.h/.cpp`。
- War3 格式模块保留原格式名小写目录，例如 `formats/w3e`、`formats/wpm`。

## 兼容性规则

- 优先兼容经典 War3 / YDWE 工作流。
- 保存 `.w3x/.w3m` 必须通过本项目 Archive 抽象，默认另存为新文件，不直接覆盖原图。
- 目录地图只作为调试、对照和导出派生产物支持，不得作为打开/保存主链路的前置依赖。
- 不确定格式字段时，读取后原样保留，避免保存时丢失未知数据。
- 对 `w3e/wpm/shd/mmp/w3i` 的修改必须能被重新读取验证。

## 参考项目使用边界

- `W3EditorOld` 的地形字段、刷子算法、shader、图标、测试地图可参考，但不得复制其 `Map` 大对象式耦合架构。
- `W3EditorOld::Terrain` 中 SoA 形式的 corner 数据可作为 `TerrainModel` 的设计参考，但必须移除 Qt、OpenGL、Bullet、资源加载等依赖。
- `TerrainCorner` 字段设计必须覆盖高度、水高、地表贴图、贴图 variation、悬崖贴图、悬崖 variation、层高、地图边界、坡道、荒芜、水面、边界、悬崖、romp、特殊装饰物等 War3 地形语义。
- `TerrainModel` 可以内部使用 SoA，但对外必须提供明确的 corner/tile 访问接口，并维持 corner 与 tile 坐标语义清晰。
- 旧项目的最终高度语义 `height + layerHeight - 2.0`、水面高度语义、地形高度上下限、层高上下限必须作为 W3E 兼容重点验证。
- `W3EditorOld` 的 `HeightOperator`、`TextureOperator`、`CliffOperator` 可作为地形工具行为参考，但新实现必须通过 `TerrainBrush`、`TerrainEditCommand`、`TerrainUndoStack` 和控制器封装。
- 新地形编辑操作必须返回脏区域，供渲染、水面、路径和保存逻辑做局部更新。
- `YDWE-master` 的价值主要是 YDWE 工作流、虚拟 MPQ、JASS/YDTrigger、SLK/BLP/StormLib、示例地图和插件体系参考。
- StormLib 是本项目 MPQ/Archive 能力的重要依赖，应优先作为 `.w3x/.w3m` 读写实现基础，并通过本项目 Archive 抽象隔离。
- 第一阶段只吸收 YDWE 的数据格式和工作流经验，不吸收注入式扩展机制。
- 第一阶段不得整体迁入 YDWE hook、Detours、MinHook、Lua 插件、YDTrigger、JAPI、War3 运行时补丁等体系。
- 现有工具只要不影响本项目目标、不破坏 64 位编译、不绕过架构边界，可以作为辅助工具、验证工具或可隔离依赖使用。
- 可优先参考或接入 `YDWE-master/OpenSource/StormLib`、`Development/Core/BlpConv`、`Development/Core/SlkLib`、`Development/Component/plugin/w3x2lni` 的格式与资源链路经验。



## 阶段门禁

- M0 必须先完成 Archive/MPQ 自主读取，能直接从 `.w3x/.w3m` 包内读取 `war3map.w3e`，再做 `w3e` 读写与一致性验证。
- M0 不允许把 `w3x2lni`、YDWE 旧 exe 或其他外部解包工具作为打开 `.w3x/.w3m` 的唯一实现或测试前置；这些工具可用于附加对照验证。
- M1 UI 只能调用文档层和地形控制器，不允许直接读写地图二进制。
- M2 必须能打开真实 `.w3x/.w3m` 样本后，目录地图路径才算调试辅助能力。
- M4 保存必须默认备份，且必须通过重新打开验证保存结果。
- 任何新格式接入都必须先有最小样本、解析结果 dump、round-trip 或保留未知字段策略。

## CLI 交互与终端编码规则

- CLI 参数必须简单好记，优先使用 `--open`、`--close`、`--file` 这类直观主参数；目录类参数统一使用短写法：`--log`/`-l` 表示日志目录，`--debug`/`-d` 表示调试输出，`--out`/`-o` 表示通用输出目录，`--dump` 表示转储目录，`--tmp` 表示临时文件目录。不使用复杂多级子命令或 `--diagnostics-dir` 这类长参数作为主入口。
- 项目刚起步阶段不保留旧 CLI 命令兼容；发现命令设计不合理时，直接删改为最新规则，避免背负无意义兼容成本。
- Windows CLI 启动时必须主动设置控制台 UTF-8 输入/输出，源码、文档、日志、JSON dump 默认使用 UTF-8。
- CLI 输出中文路径、中文文件名、错误信息和日志路径时不得乱码；涉及中文文件名的测试或手工验证必须纳入阶段验收。
- 自动化命令示例必须同时适用于 PowerShell，不依赖用户手动执行 `chcp`。

## 自动化 CLI 与测试要求

- 项目必须提供稳定 CLI，至少覆盖构建验证、Archive 列表、包内文件读取、`w3e` dump、`w3e` round-trip、打开检查。
- CLI 必须能直接处理 `.w3x/.w3m`，不得要求用户先手动或通过外部工具解包。
- CLI 可提供附加对照命令调用现有工具，但核心自动化测试不得依赖这些工具才能通过。
- CLI 必须使用稳定退出码：`0` 成功，非 `0` 失败。
- CLI 失败时必须输出明确错误信息、失败阶段和日志路径。
- 自动化测试必须能通过 CMake/CTest 或等价命令一键运行。
- 测试产物统一写入 `build/artifacts/`，日志统一写入 `build/logs/`。
- 格式 dump 优先输出 JSON，便于自动比对。

## 运行状态与日志观测

- 日志必须支持结构化 JSON Lines，CLI 和 GUI 都应支持 `--log`/`-l`、`--debug`/`-d`、`--out`/`-o`、`--dump`、`--tmp` 这组简单参数；日志、转储、临时文件和通用输出都按目录语义处理。
- 关键事件必须记录：启动、打开地图、Archive 文件列表、读取关键文件、解析失败、保存开始、备份路径、保存完成、重新打开验证结果。
- GUI 调试构建应能输出状态快照，例如 `diagnostics/state.json`，包含当前地图路径、dirty 状态、当前工具、选中坐标、最近错误。
- CLI 必须提供 `status` 或 `diagnostics` 子命令，用于读取最近一次运行状态摘要。
- MCP 工具可作为外部读取层读取日志、状态快照和 CLI 输出，但项目测试不得依赖某个特定 MCP 才能完成。
- 优先方案是稳定 CLI、结构化日志和状态快照文件；MCP 是增强观测方式，不是核心依赖。

## UI 规则

- 第一阶段使用 Qt Widgets，不引入复杂皮肤系统。
- 默认界面语言为简体中文。
- 国际化第一阶段只支持英文资源，不扩展其他语言。
- 所有新增 UI 文案必须先提供中文主文案；英文翻译作为国际化资源维护。
- 默认深色主题。
- 地形视图优先占最大空间。
- 左侧放地形工具与刷子参数，右侧放小地图和属性面板。
- 工具栏优先覆盖：新建、打开、保存、撤销、重做、选择、抬高、降低、平整、平滑、贴图、水面。

## 禁止事项

- 第一阶段不要实现触发器编辑器、物体编辑器、完整装饰物系统、完整导入管理器、JASS/Lua 编译链、YDWE 插件系统。
- 不要为了 UI 展示跳过格式读写验证。
- 不要直接覆盖用户原始地图文件。
- 不要引入 Electron 或 Web UI 作为主编辑器技术栈，除非用户明确改方向。