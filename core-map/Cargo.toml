[package]
name = "core-map"
version = "0.1.0"
edition = "2021"

[lib]
name = "core_map"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
byteorder = "1"
ceres-mpq = "0.1"
